# Extras

Standalone, tangential AWS scripts — **not** part of the OpenVPN setup.

- `userdata.sh` — an EC2 "dashboard" web page (Apache CGI) rendered via instance user data. A
  separate Linux/EC2 learning artifact; safe to ignore or delete if you only want the VPN.
